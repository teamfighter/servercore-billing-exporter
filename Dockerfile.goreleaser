FROM alpine:3.21
RUN apk add --no-cache ca-certificates \
    && adduser -D -H -u 10001 exporter
COPY servercore-billing-exporter /usr/local/bin/
USER exporter
EXPOSE 9876
ENTRYPOINT ["servercore-billing-exporter"]
